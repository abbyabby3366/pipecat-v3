services:
  pipecat-web:
    build:
      context: .
      dockerfile: Dockerfile
    image: pipecat-web-agent:latest
    container_name: pipecat-web-agent
    restart: unless-stopped
    ports:
      - "7860:7860"
    env_file:
      - .env
    environment:
      - PORT=7860
      - HOST=0.0.0.0
